/**
 * forEach - Executa a função passada por parâmetros
 */

const frameworks = ['Angular.js', 'Ember.js', 'Vue.js'];
frameworks.forEach((framework) => {
    console.log(framework);
});

//ou
frameworks.forEach((framework) => console.log(framework));
